<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="Employee">

	<resultMap id="memberDeptMap" type="com.kedu.dto.MemberDTO">
	    <!-- employee 테이블 컬럼 -->
	    <id property="empCode" column="emp_code_id" />
	    <result property="empName" column="emp_name" />
	    <result property="empEmail" column="emp_email" />
	
	    <!-- department 테이블에서 부서명만 매핑 -->
	    <association property="department" javaType="com.kedu.dto.DepartDTO">
	        <id property="deptId" column="dept_id"/>
	        <result property="deptName" column="dept_name"/>
	    </association>
	</resultMap>

	
    <select id="selectAllMembers" resultMap="memberDeptMap">
        SELECT e.emp_code_id,
    			e.emp_name,
    			e.emp_email,
    			d.dept_id,
    			d.dept_name
    	FROM employee e left join department d on e.emp_dept_id = d.dept_id
    </select>

    <select id="selectMemberById" resultType="com.kedu.dto.MemberDTO" parameterType="String">
        SELECT * FROM employee WHERE member_id = #{memberId}
    </select>


    <insert id="insertEmp" parameterType="com.kedu.dto.MemberDTO">
        INSERT INTO employee(emp_code_id, emp_per_id, emp_dept_id, emp_job_id, emp_loginId, emp_pw, emp_name, emp_rrn, emp_phone,emp_email,postcode, address1, address2, hire_date, salary, isDeft)

        VALUES(emp_seq.nextval, 501 , #{emp_dept_id}, #{emp_job_id}, #{emp_loginId}, #{emp_pw}, #{emp_name}, #{emp_rrn}, #{emp_phone}, #{emp_email}, #{postcode}, #{address1}, #{address2}, #{hire_date}, #{salary}, 'N')
    </insert>

    <update id="updateMember" parameterType="com.kedu.dto.MemberDTO">
        UPDATE employee
        SET emp_dept_id = #{emp_dept_id}, emp_job_id = #{emp_job_Id}, address1 = #{address1}
        WHERE emp_code_id = #{emp_code_id}
    </update>

    <delete id="deleteMember" parameterType="String">
        DELETE FROM employee WHERE emp_code_id = #{emp_code_id}
    </delete>

</mapper>
